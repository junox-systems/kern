<% content_for :title, "New Calendar Block" %>

<div class="max-w-xl mx-auto mt-8 px-4">
  <div class="mb-6">
    <%= link_to "← Back", calendar_blocks_path, class: "text-sm text-base-content/60 hover:text-primary transition-colors" %>
    <h1 class="text-2xl font-display font-semibold text-base-content mt-2">New Calendar Block</h1>
  </div>

  <div class="card bg-base-100 border border-base-300 shadow-sm p-6">
    <%= render "form", calendar_block: @calendar_block %>
  </div>
</div>
